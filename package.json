{
  "name": "my-electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . Wap-Forum-Overlay --platform=win32 --arch=x64 --overwrite --icon=tray-icon.ico --out=dist",
    "pack-ios": "electron-packager . Wap-Forum-Overlay --platform=darwin --arch=x64 --icon=icon.icns --out=dist"
  },
  "devDependencies": {
    "electron": "^29.4.6",
    "electron-packager": "^17.1.2"
  }
}
